apiVersion: v1
kind: Service
metadata:
  name: mirai-frontend
  namespace: mirai
  labels:
    app: mirai-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: mirai-frontend
